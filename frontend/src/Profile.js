import React, {useState} from 'react';

const Profile = () => {
    const [currentUser, setUser] = useState({})

    return (
        <div>
            WTF
        </div>
    );
};

export default Profile;